import "./Navbar.css";

const Navbar = () => {
  return (
    <nav className="navbar">
      <h1>Name</h1>
      <div className="links">
        <button onClick={() => {}}>Home</button>
      </div>
    </nav>
  );
};

export default Navbar;
